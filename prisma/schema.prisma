generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  userId   String @id @default(uuid()) // UUID generation handled by Prisma
  username String @unique
  email    String @unique
  password String

  @@map("users")
}
